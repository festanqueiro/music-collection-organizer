# TODO — Ideas

Feature ideas and requests not yet scoped/implemented. Distinct from
`TODO.md`, which tracks known bugs/gaps in what's already built.

Nothing here right now — the two items that were here (analysis progress
indicator, Settings modal with backups) are implemented on the
`v1-library-organizer` branch/PR. See that branch's `TODO.md` for what's
still outstanding there.
